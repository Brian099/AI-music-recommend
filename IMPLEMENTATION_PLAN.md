# Embeat 音乐资源管理工具与播放器 改造实施计划 (已更新-含Docker路径挂载部署)

本项目将重构升级为**前后台分离**的 Web 音乐系统：**播放器前台（免密听歌）** 与 **管理后台（密码拦截）**，集资源管理、真假无损鉴定、智能去重、**4500万预置数据包可选检测与优雅降级**、**Docker 路径卷映射开箱即用**、集成 LDDC 歌词（本地优先+播放在线保底）、自动/手动刮削（数据库入库+文件物理写入）、多维分类浏览、**本地目录零等待直取播放**、**微批次增量即时入库（断点秒级续扫）**、**硬件加速与保留单核防护**、**极致 Web 播放器 UI/UX** 与 AI 近似推荐于一体。

---

## 核心架构与 Docker 卷映射

```
                                  ┌───────────────────────────┐
                                  │      Docker 宿主机 (Host) │
                                  └─────────────┬─────────────┘
                                                │ 目录卷映射 (Volume Mounts)
         ┌──────────────────────────────────────┼──────────────────────────────────────┐
         │                                      │                                      │
  - ./music:/music                     - ./embeat_qdrant_db                    - ./data:/app/data
  (宿主机音乐文件夹)                    :/qdrant/storage                       (SQLite 数据库与缓存)
         │                             (4500万数据包，可选挂载)                │
         ▼                                      ▼                                      ▼
┌─────────────────────────────────┐   ┌──────────────────────────────────┐   ┌──────────────────────────────────┐
│  embeat-api 容器                │   │  embeat-qdrant 容器              │   │  SQLite 数据库                   │
│  • 播放器前台 / (0秒点播)       │◄──┤  • 4500万向量加速与海量召回     │◄──┤  • 目录树/歌手/专辑/去重/刮削  │
│  • 管理后台 /admin (密码拦截)   │   │  • 若未挂载则自动自建纯本地存储  │   │  • 持久化于宿主机 data 目录      │
└─────────────────────────────────┘   └──────────────────────────────────┘   └──────────────────────────────────┘
```

---

## 核心功能与机制设计

### A. Docker 卷映射与一键部署设计 (`docker-compose.yml`)
1. **4500万预置数据包挂载 (可选)**：
   - 挂载项：`- ./embeat_qdrant_db:/qdrant/storage`
   - 宿主机存在 `embeat_qdrant_db` 时直接映射进 Qdrant 容器即可激活 4500万向量极速复用与海量召回；若不挂载，Qdrant 自动新建空白存储，降级为纯本地模式。
2. **音乐文件与数据持久化挂载**：
   - 挂载项：`- /your/local/music:/music`（映射用户音乐库）
   - 挂载项：`- ./data:/app/data`（持久化 SQLite 关系库 `library.db` 及去重/刮削缓存）
3. **环境变量配置 (`environment`)**：
   - `ADMIN_PASSWORD=admin123`（后台登录密码）
   - `QDRANT_URL=http://qdrant:6333`
   - `MUSIC_DIR=/music`

### B. 4500万预置数据包可选检测与优雅降级
- 模式一（挂载数据包）：自动开启 4500万流行歌曲向量秒级复用（提升 100 倍扫描速度）与海量 AI 漫游。
- 模式二（未挂载数据包）：自动降级为纯本地自建模式，依靠本地 CPU/GPU 提取特征建立索引。

### C. LDDC 歌词双层智能混合获取机制
- **本地/已刮削缓存优先 (0 毫秒延迟)**：优先读取同级 `.lrc` 或数据库歌词。
- **播放时在线保底 + 自动异步落盘**：若无歌词，前台播放瞬间后台调用 LDDC 实时获取并自动异步落盘。

### D. 硬件性能最大化加速 + 保留单核防护
- 线程数限制为 $\text{Max Workers} = \max(1, N - 1)$，**保留 1 个 CPU 核心**防止系统卡死。支持 CUDA GPU 加速。

### E. 播放器前台极致 UI / UX 细节设计
- **动态封面色彩提取背景** Canvas 动态弥散渐变。
- **高端音质与真假无损徽章**：🟢 `FLAC 24bit/96kHz (真无损)`、🟡 `MP3 320k`、🔴 `FLAC ⚠️ 假无损`。
- **一键“⚡ AI 漫游电台”与无缝预加载 (Gapless Playback)** & **系统级媒体按键联动 (MediaSession API)**。

### F. 断点秒级续扫与零重复计算
- 内存 Set 进行 $O(1)$ 高速哈希匹配，已处理过的歌曲在几毫秒内全部跳过。

### G. 按目录播放零等待 (Direct File System Traversal Playback)
- 打开任意本地文件夹，后端直接快速遍历文件系统（`os.scandir`），**0秒等待秒开播放**。

### H. 前后台隔离与密码拦截
- **播放器前台 (`/`)**：免密公开访问；**管理后台 (`/admin`)**：密码拦截鉴权。

---

## 详细模块划分与文件规划

### 配置文件与 Docker 模块

1. **[MODIFY] `docker-compose.yml`**
   - 配置 `./embeat_qdrant_db:/qdrant/storage` 可选映射、`./data:/app/data` 数据库挂载、`ADMIN_PASSWORD` 环境变量。

2. **[MODIFY] `Dockerfile`**
   - 打包主服务、FFmpeg / Mutagen / Librosa 环境与内嵌 LDDC 模块。

### 后端模块

1. **[MODIFY] `infer/app.py`**
   - 路径感应与数据包检测，集成 Auth 中间件与 API 路由。

2. **[NEW] `infer/scraper.py`**
   - 批量刮削与 LDDC 在线抓取引擎。

3. **[NEW] `infer/auth.py`**
   - 管理员密码 Token 签发与 FastAPI Auth 中间件。

4. **[NEW] `infer/library_db.py`**
   - SQLite 数据库管理类。

5. **[NEW] `infer/quality_analyzer.py`**
   - STFT 频谱检测器。

6. **[NEW] `infer/dedupe.py`**
   - 智能音频去重检测器。

### 前端模块

1. **[MODIFY] `infer/index.html`**：播放器前台 UI。
2. **[NEW] `infer/admin.html`**：管理后台 UI。

---

## 验证与测试计划

### 自动化与单元测试
- **Docker 构建与运行测试**：使用 `docker compose up -d` 验证容器启动、存储映射与服务通信。

### 手动功能验证
- 验证 Docker 挂载数据包与不挂载数据包时的启动情况。
