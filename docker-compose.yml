version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: embeat-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      # 直接挂载本地已有的数据库文件夹，实现向量库数据无缝继承
      - ./storage:/qdrant/storage
    restart: always

  embeat-api:
    image: brian9909/ai-music-recommend:latest
    container_name: embeat-api-service
    ports:
      - "8000:8000"
    volumes:
      # 挂载宿主机测试音乐文件夹到容器内的 /music，供离线提取与试听
      # 注意：checkpoints 已打包进镜像，无需挂载（挂载会覆盖镜像内文件）
      - ./测试音乐:/music
    environment:
      - QDRANT_URL=http://qdrant:6333
      - MUSIC_DIR=/music
    depends_on:
      - qdrant
    restart: always
